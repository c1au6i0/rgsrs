% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_substance.R
\name{gsrs_substance}
\alias{gsrs_substance}
\title{Fetch a GSRS substance record by UNII}
\usage{
gsrs_substance(unii, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{unii}{Character vector of one or more UNII codes
(e.g., \code{"R16CO5Y76E"} for aspirin).}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups when
\code{unii} has multiple entries. Default \code{0.5}.}
}
\value{
A data frame with the same columns as \code{\link[=gsrs_search]{gsrs_search()}}, with one row
per input UNII.  Rows for unrecognised UNIIs will contain \code{NA} except for
the \code{query} column (which is always set to the input UNII).  Returns
\code{NULL} on error (with a warning).
}
\description{
Retrieves the top-level metadata for a single substance identified by its
UNII (Unique Ingredient Identifier / approval ID). Internally this performs
a filtered search using \verb{root_approvalID:<unii>}.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_substance("R16CO5Y76E")  # aspirin
  if (!is.null(out)) print(out)
}
}
\seealso{
\code{\link[=gsrs_search]{gsrs_search()}}, \code{\link[=gsrs_names]{gsrs_names()}}, \code{\link[=gsrs_codes]{gsrs_codes()}}
}
