% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_all.R
\name{gsrs_all}
\alias{gsrs_all}
\title{Retrieve comprehensive GSRS data for a set of UNIIs}
\usage{
gsrs_all(unii, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{unii}{Character vector of one or more UNII codes.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups.
Default \code{0.5}.}
}
\value{
A named list with five elements:
\describe{
\item{substance}{Data frame from \code{\link[=gsrs_substance]{gsrs_substance()}}.}
\item{names}{Data frame from \code{\link[=gsrs_names]{gsrs_names()}}.}
\item{codes}{Data frame from \code{\link[=gsrs_codes]{gsrs_codes()}}.}
\item{structure}{Data frame from \code{\link[=gsrs_structure]{gsrs_structure()}}.}
\item{hierarchy}{Data frame from \code{\link[=gsrs_hierarchy]{gsrs_hierarchy()}}.}
}
Returns \code{NULL} on error (with a warning).
}
\description{
Convenience wrapper that calls \code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_names]{gsrs_names()}},
\code{\link[=gsrs_codes]{gsrs_codes()}}, \code{\link[=gsrs_structure]{gsrs_structure()}}, and \code{\link[=gsrs_hierarchy]{gsrs_hierarchy()}} in sequence and
returns a named list containing all five data frames. Each sub-function uses
\code{with_graceful_exit} internally, so partial failures return \code{NULL} for that
element without aborting the whole call.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_all("R16CO5Y76E")  # aspirin
  if (!is.null(out)) {
    print(out$substance)
    print(head(out$names))
    print(head(out$codes))
    print(out$structure[, c("smiles", "formula", "mwt", "inchi_key")])
    print(out$hierarchy[, c("depth", "type", "approval_id", "name")])
  }
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_names]{gsrs_names()}}, \code{\link[=gsrs_codes]{gsrs_codes()}},
\code{\link[=gsrs_structure]{gsrs_structure()}}, \code{\link[=gsrs_hierarchy]{gsrs_hierarchy()}}
}
