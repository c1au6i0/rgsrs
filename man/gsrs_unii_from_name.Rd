% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_unii_from_name.R
\name{gsrs_unii_from_name}
\alias{gsrs_unii_from_name}
\title{Look up UNII codes for substance names}
\usage{
gsrs_unii_from_name(names, top = 1L, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{names}{Character vector of substance names to resolve.}

\item{top}{Integer. Maximum number of candidate records to consider per
name query. Default \code{1} returns only the top hit. Increase to inspect
multiple candidates.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups.
Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{unii}{The UNII / approval ID of the matched substance.}
\item{preferred_name}{Preferred display name in GSRS.}
\item{substance_class}{Substance class (e.g., \code{"chemical"}).}
\item{status}{Record status.}
\item{uuid}{Internal GSRS UUID.}
\item{date_retrieved}{Date the response was received.}
\item{query}{The name supplied by the caller.}
}
Unresolved names produce a row of \code{NA}s with \code{query} set. Returns \code{NULL}
on error (with a warning).
}
\description{
For each supplied name, queries GSRS using \verb{root_names:<name>} and returns
the best-matching UNII together with the preferred substance name and
substance class. This is useful for converting common or systematic names
to the canonical FDA UNII identifier.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_unii_from_name(c("aspirin", "ibuprofen"))
  if (!is.null(out)) print(out)
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_search]{gsrs_search()}}, \code{\link[=gsrs_names]{gsrs_names()}}
}
