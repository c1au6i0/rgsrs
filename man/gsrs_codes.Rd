% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_codes.R
\name{gsrs_codes}
\alias{gsrs_codes}
\title{Retrieve external codes and identifiers for GSRS substances}
\usage{
gsrs_codes(unii, code_system = NULL, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{unii}{Character vector of one or more UNII codes.}

\item{code_system}{Character vector of code systems to filter on
(e.g., \code{c("CAS", "PUBCHEM")}). Case-insensitive matching. Pass \code{NULL}
(default) to return all code systems.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups when
\code{unii} has multiple entries. Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{code_system}{External database / code system name
(e.g., \code{"CAS"}, \code{"PUBCHEM"}, \code{"ChEMBL"}, \code{"WHO-ATC"}).}
\item{code}{The identifier in that system.}
\item{type}{\code{"PRIMARY"} or \code{"ALTERNATIVE"}.}
\item{url}{URL to the external record (when available).}
\item{comments}{Additional context for the code (e.g., ATC path).}
\item{is_classification}{Logical; \code{TRUE} for classification codes.}
\item{uuid}{Internal GSRS UUID for the code record.}
\item{date_retrieved}{Date the response was received.}
\item{query}{The UNII supplied by the caller.}
}
Returns \code{NULL} on error (with a warning).
}
\description{
For each supplied UNII, calls \verb{GET /api/v1/substances(<UNII>)/codes} and
returns all registered cross-references as a tidy data frame. These include
CAS numbers, PubChem CIDs, ChEMBL IDs, WHO-ATC codes, NDF-RT codes,
DrugBank IDs, and many more.
}
\examples{
\donttest{
  Sys.sleep(2)
  # All codes for aspirin
  out <- gsrs_codes("R16CO5Y76E")
  if (!is.null(out)) print(head(out))

  Sys.sleep(2)
  # Only CAS and PubChem codes
  out_cas <- gsrs_codes("R16CO5Y76E", code_system = c("CAS", "PUBCHEM"))
  if (!is.null(out_cas)) print(out_cas)
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_names]{gsrs_names()}}, \code{\link[=gsrs_search]{gsrs_search()}}
}
