% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_hierarchy.R
\name{gsrs_hierarchy}
\alias{gsrs_hierarchy}
\title{Retrieve the relationship hierarchy for GSRS substances}
\usage{
gsrs_hierarchy(unii, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{unii}{Character vector of one or more UNII codes.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups when
\code{unii} has multiple entries. Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{node_id}{Node identifier within the hierarchy tree (string index).}
\item{parent_id}{Parent node identifier (\code{"#"} for root nodes).}
\item{depth}{Depth in the tree (0 = root).}
\item{type}{Node type (e.g., \code{"ROOT"}, \code{"ACTIVE MOIETY"},
\code{"SALT/SOLVATE"}).}
\item{text}{Human-readable label including UNII and name.}
\item{expandable}{Logical; \code{TRUE} if node has children.}
\item{approval_id}{UNII of the substance at this node.}
\item{name}{Preferred name at this node.}
\item{ref_uuid}{Internal GSRS UUID of the related substance.}
\item{substance_class}{Substance class at this node.}
\item{deprecated}{Logical; \code{TRUE} if the node substance is deprecated.}
\item{date_retrieved}{Date the response was received.}
\item{query}{The UNII supplied by the caller.}
}
Returns \code{NULL} on error (with a warning).
}
\description{
For each supplied UNII, calls \verb{GET /api/v1/substances(<UNII>)/@hierarchy}
and returns the flat parent/child relationship tree as a tidy data frame.
This is useful for navigating relationships such as salt forms to free base,
active metabolites, or component substances.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_hierarchy("R16CO5Y76E")  # aspirin
  if (!is.null(out)) print(out[, c("depth", "type", "approval_id", "name")])
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_all]{gsrs_all()}}
}
