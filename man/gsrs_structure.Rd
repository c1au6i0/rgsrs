% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_structure.R
\name{gsrs_structure}
\alias{gsrs_structure}
\title{Retrieve chemical structure data for GSRS substances}
\usage{
gsrs_structure(unii, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{unii}{Character vector of one or more UNII codes.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between individual lookups when
\code{unii} has multiple entries. Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{smiles}{Canonical SMILES string.}
\item{formula}{Molecular formula (e.g., \code{"C9H8O4"}).}
\item{mwt}{Molecular weight (numeric).}
\item{inchi_key}{Standard InChIKey.}
\item{inchi}{Full InChI string.}
\item{stereochemistry}{Stereochemistry descriptor (e.g., \code{"ACHIRAL"},
\code{"RACEMIC"}, \code{"ABSOLUTE"}).}
\item{optical_activity}{Optical activity (e.g., \code{"UNSPECIFIED"}, \code{"(+)"},
\code{"(-)"}).}
\item{charge}{Formal charge (integer).}
\item{stereo_centers}{Number of stereocenters.}
\item{defined_stereo}{Number of defined stereocenters.}
\item{ez_centers}{Number of E/Z double-bond stereocenters.}
\item{molfile}{MDL molfile as a string.}
\item{date_retrieved}{Date the response was received.}
\item{query}{The UNII supplied by the caller.}
}
Non-chemical substances (proteins, polymers, etc.) return a row of \code{NA}s
with \code{query} set. Returns \code{NULL} on error (with a warning).
}
\description{
For each supplied UNII, fetches the full substance record from
\verb{GET /api/v1/substances(<UNII>)} and extracts the embedded \code{structure}
object, returning chemical identifiers and properties as a tidy data frame.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_structure("R16CO5Y76E")  # aspirin
  if (!is.null(out)) print(out[, c("smiles", "formula", "mwt", "inchi_key")])
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_structure_search]{gsrs_structure_search()}}, \code{\link[=gsrs_names]{gsrs_names()}},
\code{\link[=gsrs_codes]{gsrs_codes()}}
}
