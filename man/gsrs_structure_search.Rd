% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_structure_search.R
\name{gsrs_structure_search}
\alias{gsrs_structure_search}
\title{Search GSRS by chemical structure}
\usage{
gsrs_structure_search(
  smiles,
  type = c("sub", "sim", "exact", "flex"),
  cutoff = 0.8,
  top = 10L,
  verbose = TRUE
)
}
\arguments{
\item{smiles}{Character string. A valid SMILES or SMARTS string describing
the query structure (e.g., \code{"CC(=O)Oc1ccccc1C(=O)O"} for aspirin).}

\item{type}{Character string. Search type. One of:
\describe{
\item{\code{"sub"}}{Substructure search (default). Returns all substances
whose structure contains the query as a substructure.}
\item{\code{"sim"}}{Similarity search. Returns substances with Tanimoto
similarity >= \code{cutoff}. Use \code{cutoff} to control threshold.}
\item{\code{"exact"}}{Exact structure match (tautomer-aware, stereo-sensitive).}
\item{\code{"flex"}}{Flexible (disconnected moiety) search; stereo-insensitive.}
}}

\item{cutoff}{Numeric in \verb{[0, 1]}. Tanimoto similarity cutoff for
\code{type = "sim"}. Default \code{0.8}. Ignored for other search types.}

\item{top}{Integer. Maximum number of records to return. Default \code{10}.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}
}
\value{
A data frame with the same columns as \code{\link[=gsrs_search]{gsrs_search()}}, plus a
\code{query_smiles} column recording the input SMILES. Returns \code{NULL} on error
(with a warning).
}
\description{
Searches the FDA Global Substance Registration System for substances matching
a chemical structure query supplied as a SMILES string. Supports
substructure, similarity, exact-match, and flexible (disconnected moiety)
search types.
}
\examples{
\donttest{
  Sys.sleep(2)
  # Exact match for aspirin
  out <- gsrs_structure_search("CC(=O)Oc1ccccc1C(=O)O", type = "exact")
  if (!is.null(out)) print(out[, c("approval_id", "preferred_name")])

  Sys.sleep(2)
  # Similarity search
  out_sim <- gsrs_structure_search("CC(=O)Oc1ccccc1C(=O)O",
                                   type = "sim", cutoff = 0.7, top = 5)
  if (!is.null(out_sim)) print(out_sim[, c("approval_id", "preferred_name")])
}
}
\seealso{
\code{\link[=gsrs_structure]{gsrs_structure()}}, \code{\link[=gsrs_search]{gsrs_search()}}
}
