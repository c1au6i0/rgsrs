% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_browse.R
\name{gsrs_browse}
\alias{gsrs_browse}
\title{Browse all substance records in GSRS}
\usage{
gsrs_browse(top = 10L, skip = 0L, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{top}{Integer. Maximum number of records to return per request.
Default \code{10}. Set to \code{NULL} or \code{Inf} to fetch all records (paginates
automatically; large result sets will be slow).}

\item{skip}{Integer. Number of records to skip (offset). Default \code{0}.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between paginated requests when
\code{top = Inf}. Default \code{0.5}.}
}
\value{
A data frame with the same columns as \code{\link[=gsrs_search]{gsrs_search()}}.
Returns \code{NULL} on error (with a warning).
}
\description{
Retrieves a paginated list of all substance records from
\code{GET /api/v1/substances}. Useful for bulk workflows or building a local
catalogue. Use \code{top} and \code{skip} to page through the ~170,000 available
records, or set \code{top = Inf} to fetch all (slow — use with care).
}
\examples{
\donttest{
  Sys.sleep(2)
  # Fetch the first 5 substance records
  out <- gsrs_browse(top = 5, verbose = FALSE)
  if (!is.null(out)) print(out[, c("approval_id", "preferred_name",
                                    "substance_class")])
}
}
\seealso{
\code{\link[=gsrs_search]{gsrs_search()}}, \code{\link[=gsrs_substance]{gsrs_substance()}}
}
