% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_vocabularies.R
\name{gsrs_vocabularies}
\alias{gsrs_vocabularies}
\title{Retrieve controlled vocabulary terms from GSRS}
\usage{
gsrs_vocabularies(top = NULL, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{top}{Integer. Maximum number of vocabulary \emph{domains} to return per
request. Default \code{NULL} fetches all domains (paginates automatically).}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between paginated requests.
Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{domain}{Vocabulary domain name (e.g., \code{"NAME_TYPE"},
\code{"SUBSTANCE_CLASS"}, \code{"RELATIONSHIP_TYPE"}).}
\item{term_type}{Vocabulary term type identifier.}
\item{editable}{Logical; \code{TRUE} if the vocabulary can be extended.}
\item{filterable}{Logical; \code{TRUE} if the vocabulary supports filtering.}
\item{value}{The controlled term value (used in the API/data).}
\item{display}{Human-readable display label for the term.}
\item{hidden}{Logical; \code{TRUE} if the term is hidden from the UI.}
\item{selected}{Logical; \code{TRUE} if the term is selected by default.}
\item{date_retrieved}{Date the response was received.}
}
Returns \code{NULL} on error (with a warning).
}
\description{
Fetches all (or a page of) controlled vocabulary entries from
\code{GET /api/v1/vocabularies}. The result is one row per vocabulary term,
with the parent domain and type attached to every row. This is useful for
understanding allowed values for fields such as name type, substance class,
relationship type, code system, and more.
}
\examples{
\donttest{
  Sys.sleep(2)
  vocab <- gsrs_vocabularies(verbose = FALSE)
  if (!is.null(vocab)) {
    # See all name type values
    print(vocab[vocab$domain == "NAME_TYPE", c("value", "display")])
  }
}
}
\seealso{
\code{\link[=gsrs_search]{gsrs_search()}}, \code{\link[=gsrs_codes]{gsrs_codes()}}
}
