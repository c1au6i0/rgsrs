% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsrs_search.R
\name{gsrs_search}
\alias{gsrs_search}
\title{Search the GSRS substance database}
\usage{
gsrs_search(query, top = 10L, skip = 0L, verbose = TRUE, delay = 0.5)
}
\arguments{
\item{query}{Character string. The search query. Supports:
\itemize{
\item Free text (e.g., \code{"aspirin"})
\item Lucene field syntax (e.g., \code{"root_names:aspirin"},
\code{"root_approvalID:R16CO5Y76E"})
\item Wildcards (\code{*}, \verb{?}) as per GSRS documentation.
}}

\item{top}{Integer. Maximum number of records to return per request.
Default \code{10}. Use \code{NULL} or \code{Inf} to attempt to retrieve all records
(paginates automatically; large result sets may be slow).}

\item{skip}{Integer. Number of records to skip (offset). Default \code{0}.}

\item{verbose}{Logical. If \code{TRUE}, emit progress messages. Default \code{TRUE}.}

\item{delay}{Numeric. Seconds to wait between paginated requests.
Default \code{0.5}.}
}
\value{
A data frame with columns:
\describe{
\item{uuid}{Internal GSRS UUID of the substance.}
\item{approval_id}{FDA UNII / approval ID.}
\item{preferred_name}{Preferred display name.}
\item{substance_class}{Substance class (e.g., \code{"chemical"},
\code{"structurallyDiverse"}).}
\item{status}{Record status (e.g., \code{"approved"}).}
\item{definition_type}{\code{"PRIMARY"} or \code{"ALTERNATIVE"}.}
\item{definition_level}{\code{"COMPLETE"} or \code{"INCOMPLETE"}.}
\item{version}{Record version string.}
\item{names_url}{URL to retrieve all names for this substance.}
\item{codes_url}{URL to retrieve all codes for this substance.}
\item{self_url}{Full URL for this substance record.}
\item{date_retrieved}{Date the response was received from the server.}
}
Returns \code{NULL} on error (with a warning).
}
\description{
Searches the FDA Global Substance Registration System (GSRS) using a
free-text or Lucene-style field query. Returns a tidy data frame of
matching substance records with key metadata fields.
}
\examples{
\donttest{
  Sys.sleep(2)
  out <- gsrs_search("aspirin", top = 5)
  if (!is.null(out)) print(head(out))
}
}
\seealso{
\code{\link[=gsrs_substance]{gsrs_substance()}}, \code{\link[=gsrs_names]{gsrs_names()}}, \code{\link[=gsrs_codes]{gsrs_codes()}}
}
